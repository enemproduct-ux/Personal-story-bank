// Copy this file to config.js and fill in your own Supabase project's values.
// Find them in your Supabase dashboard under Project Settings → API.
//
//   cp config.example.js config.js
//
// config.js is listed in .gitignore on purpose — every person who forks this
// repo points it at their own Supabase project, so it should never be
// committed (yours or anyone else's).

window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";

// Optional: the name shown in the header. Make it yours.
window.APP_NAME = "Story Bank";
